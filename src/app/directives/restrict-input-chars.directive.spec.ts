import { RestrictCharsDirective } from './restrict-input-chars.directive';

describe('RestrictSpecialCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictCharsDirective();
    expect(directive).toBeTruthy();
  });
});
